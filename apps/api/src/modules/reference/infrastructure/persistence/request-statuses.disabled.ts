// Intentionally empty. `request_statuses` is owned by the request-statuses module
// (RequestStatusEntity). This file cannot be deleted on the current mount, so it is
// left with no entity to avoid a duplicate MikroORM mapping.
export {};
