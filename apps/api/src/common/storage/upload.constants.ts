/** Soft threshold: files above this use multipart (browser). */
export const MULTIPART_THRESHOLD_BYTES = 50 * 1024 * 1024;

/** Hard cap for any single uploaded object (submissions / discussions). */
export const UPLOAD_MAX_BYTES = 5 * 1024 * 1024 * 1024;
